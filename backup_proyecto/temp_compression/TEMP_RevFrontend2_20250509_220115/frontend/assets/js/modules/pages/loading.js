// frontend/assets/js/modules/pages/landing.js

// Importamos la función que carga el header y footer
import { loadHeaderFooter } from "../utils/include.js";

// Esta función se ejecuta cuando se carga la página
export function initPage() {
  loadHeaderFooter();  // Carga el header y footer en sus respectivos <div>
}
